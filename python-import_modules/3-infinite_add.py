#!/usr/bin/python3
import sys
if __name__ == "__main__":
    argnum = sys.argv
    arglen = len(argnum)
    argsum = 0

    if arglen > 1:
        for i in range(1, arglen)
        argsum += int(argnum[i])
    print(argsum)
